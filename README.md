# andera_browser_agent_assignment

1. Set your ANTHROPIC_API_KEY as an environment variable.
2. Install dependencies:
  - numpy
  - pandas
  - playwright
  - playwright install chromium
  - anthropic

4. Simply specify your user_prompt, task_name, starting_url, and input_csv_path in the main function in main.py and run the file.
